{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://gorp.dev/specs/runtime/run-record.schema.json",
  "title": "Gorp Run Record",
  "description": "Durable, auditable record of one node run, assembled once by `gorp run` at the end of the run and never edited afterwards (append-only, hashed into the audit chain). finalStatus records the EXECUTION outcome (current runtime produces 'succeeded' or 'failed'); the later review verdict lives only in review-decision.schema.json and the promotion result only in promotion-record.schema.json. controlDecisions EXPLAINS the control steps, not merely the outputs.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "runId",
    "graphId",
    "nodeId",
    "projectId",
    "governanceVersion",
    "baseCommit",
    "workerAdapter",
    "finalStatus",
    "startedAt"
  ],
  "properties": {
    "schemaVersion": { "type": "integer", "const": 1 },
    "runId": { "$ref": "#/$defs/stableId" },
    "graphId": { "$ref": "#/$defs/stableId" },
    "nodeId": { "$ref": "#/$defs/stableId" },
    "projectId": { "$ref": "#/$defs/stableId" },
    "governanceVersion": {
      "type": "string",
      "minLength": 1,
      "description": "Resolved Gorp governance/doctrine version in force for this run."
    },
    "baseCommit": { "type": "string", "minLength": 7 },
    "baseline": {
      "description": "Immutable repository baseline captured at run start (GOS-33): git HEAD + branch/tag refs + committed tree hash, or a file-hash list for non-git targets. Verified unchanged before promotion.",
      "oneOf": [
        { "$ref": "#/$defs/gitBaseline" },
        { "$ref": "#/$defs/filesBaseline" }
      ]
    },
    "workerAdapter": { "type": "string", "minLength": 1 },
    "sandboxIdentity": {
      "type": "string",
      "description": "Opaque sandbox identifier (e.g. worktree branch). Not created in Wave A."
    },
    "workerResultRef": { "type": "string" },
    "workerResult": { "type": "object" },
    "gateRecordRef": { "type": "string" },
    "gateRecord": { "type": "object" },
    "promotionResult": {
      "type": "object",
      "additionalProperties": false,
      "description": "Unused by the current runtime: promotion is recorded in the separate immutable promotion-record.schema.json (the run record is never updated after the run ends). Retained for record-shape compatibility.",
      "properties": {
        "status": { "type": "string", "enum": ["promoted", "not_promoted", "conflict"] },
        "resultCommit": { "type": "string" },
        "detail": { "type": "string" }
      }
    },
    "controlDecisions": {
      "type": "array",
      "description": "Ordered record of WHY the run took each control step (the explanatory audit).",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["decision", "reasonCode"],
        "properties": {
          "decision": { "type": "string", "minLength": 1 },
          "reasonCode": { "type": "string", "pattern": "^[A-Z][A-Z0-9_]*$" },
          "reasonText": { "type": "string" },
          "at": { "type": "string", "format": "date-time" }
        }
      }
    },
    "stateTransitions": {
      "type": "array",
      "items": { "type": "object" },
      "description": "Copies/refs of the transition records relevant to this run."
    },
    "finalStatus": {
      "type": "string",
      "enum": ["succeeded", "failed", "blocked", "rejected", "cancelled"]
    },
    "startedAt": { "type": "string", "format": "date-time" },
    "profile": {
      "type": "object",
      "additionalProperties": false,
      "description": "Assembled worker profile stamped at dispatch (optional — only present when a persona is assigned).",
      "properties": {
        "persona": { "type": "string", "minLength": 1 },
        "model": { "type": "string", "minLength": 1 },
        "role": { "type": "string", "minLength": 1 },
        "promptHash": {
          "type": "string",
          "pattern": "^[a-f0-9]{64}$",
          "description": "sha256 of the resolved worker profile (persona body/system-prompt + model) — deterministic lifecycle evidence for the review decision (GOS-46)."
        }
      }
    },
    "diagnostics": {
      "type": "object",
      "additionalProperties": true,
      "description": "Worker-spawn diagnostics (GOS-57): persisted when a worker fails before producing a result (e.g. a start-up hang) — cmd, cwd, model, persona, promptLen, args (persona body redacted), and the startup/run timeout windows. No full prompt."
    },
    "endedAt": { "type": "string", "format": "date-time" },
    "usage": {
      "type": "object",
      "additionalProperties": false,
      "description": "Per-run worker usage (GOS-55): tokens and cost are stamped only when the worker reports them (never estimated); durationMs is always present when usage is present.",
      "properties": {
        "tokensIn": { "type": "integer", "minimum": 0 },
        "tokensOut": { "type": "integer", "minimum": 0 },
        "tokensTotal": { "type": "integer", "minimum": 0 },
        "costUsd": { "type": "number", "minimum": 0 },
        "durationMs": { "type": "number", "minimum": 0 }
      }
    }
  },
  "$defs": {
    "stableId": {
      "type": "string",
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$"
    },
    "gitBaseline": {
      "type": "object",
      "additionalProperties": false,
      "required": ["kind", "head", "refs", "treeHash", "capturedAt"],
      "properties": {
        "kind": { "const": "git" },
        "head": { "type": "string", "pattern": "^[0-9a-f]{40}$" },
        "refs": {
          "type": "object",
          "description": "Resolved branch/tag ref name -> commit sha (gorp-managed sandbox refs excluded).",
          "additionalProperties": { "type": "string" }
        },
        "treeHash": { "type": "string", "pattern": "^[0-9a-f]{40}$" },
        "capturedAt": { "type": "string", "format": "date-time" }
      }
    },
    "filesBaseline": {
      "type": "object",
      "additionalProperties": false,
      "required": ["kind", "files", "capturedAt"],
      "properties": {
        "kind": { "const": "files" },
        "files": {
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["path", "sha256"],
            "properties": {
              "path": { "type": "string" },
              "sha256": { "type": "string", "pattern": "^[0-9a-f]{64}$" }
            }
          }
        },
        "capturedAt": { "type": "string", "format": "date-time" }
      }
    }
  }
}
