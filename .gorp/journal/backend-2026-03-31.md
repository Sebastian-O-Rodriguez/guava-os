## Task #15 — Review chat input sanitization and API error consistency

Status: done
Files:

- src/components/chat.tsx
- src/components/dashboard/inline-chat.tsx
- src/app/api/chat/route.ts
- src/lib/chat-executor.ts

Tests: n/a (vitest not installed — known issue I1)

Summary:

1. **Input length limits**: Added `maxLength={500}` to the textarea in `chat.tsx` and the input in `inline-chat.tsx`.

2. **Fetch timeout**: Added `AbortController` with a 30s timeout to fetch calls in both `chat.tsx` and `inline-chat.tsx`. Both catch handlers detect `err.name === "AbortError"` and show a distinct timeout message. The `finally` block always clears the timeout timer.

3. **Consistent API error shape**: All error responses in `src/app/api/chat/route.ts` now return `{ message: string, error: true }`. Three paths updated: empty messages, blank/missing user content, and the outer 500 catch. Also added server-side input trim before classification.

4. **Bound LLM output values** in `src/lib/chat-executor.ts`:
   - Category name: `parsed.data.name.slice(0, 50)` before `createCategory`
   - Goal target: `Math.min(Math.max(parsed.data.target, 0), 100_000)` before `upsertGoal`
   - Run miles: `Math.min(Math.max(parsed.data.miles, 0), 1_000)` before `createLog`
   - Response messages updated to use the safe values.

5. **Trim + length validation server-side**: `route.ts` now trims the extracted user content and rejects requests over 500 chars with a `400` and the standard error shape.

`tsc --noEmit` passes clean.
