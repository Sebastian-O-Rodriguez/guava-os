# Frontend Journal — 2026-04-05

## Task — Home Screen Layout Lock (Sprint 9, Wave 1)

Status: done
TypeScript: 0 errors (npx tsc --noEmit clean)

### What was done

Implemented locked home screen layout per CTO directive. No improvisation.

**New component: `DailyCard`** (`components/ui/card-templates.tsx`)
- Left column: tile grid with explicit XStack rows, max 3 columns
- Right column: doughnut chart (visual anchor, flex=1)
- Breakpoint at innerWidth < 406px: stacks vertically (tiles then doughnut)
- No flexWrap — tiles arranged into programmatic rows
- Doughnut receives remaining horizontal space, minimum 140px

**New layout constants** (`lib/layout.ts`)
- `DAILY_TILE_COLUMNS = 3`
- `DAILY_DOUGHNUT_MIN = 140`
- `DAILY_TWO_COL_MIN = 406`

**Updated home page** (`app/index.tsx`)
- New order: Nav → Header/Date → ChatInput → DailyCard → WeeklyCard
- DailyCard: 4 daily habit tiles + nutrition doughnut (single card)
- WeeklyCard: 3 weekly habit tiles via CollectionCard (flexWrap OK)
- Progress bars removed from home — doughnut is sole nutrition viz
- SummaryBreakdownCard no longer used on home (replaced by DailyCard)

### Key decisions
- Nutrition progress bars removed (redundancy rule: no cross-card duplication)
- Chat moved above cards (per directive)
- Header/date restored above chat bar
- DailyCard wraps both habits and nutrition in purple Theme (doughnut uses theme tokens, tiles use ACCENT hex)

### Files changed
- `lib/layout.ts` — added DailyCard constants
- `components/ui/card-templates.tsx` — added DailyCard template
- `app/index.tsx` — new page structure

### Blockers
None. Wave 1 complete. Wave 2 (data connection) not started.
