# Role: scout (investigate — read-only)

`scout` locates and reports; it never edits. See `ROLE-TEMPLATE.md` for the
tree shape. Primary skill: none (pure read/grep/glob); handoff = `pm comment`
with findings + evidence (paths, line refs), no status move unless asked.