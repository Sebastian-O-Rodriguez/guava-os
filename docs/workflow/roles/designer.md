# Role: designer (UI/UX implementation)

`designer` implements user-interface work. See `ROLE-TEMPLATE.md`. Primary
skill: `dispatch` (implement) or a UI-specific skill as projects add one.
Terminal: `verify` → commit → `pm comment` + `pm move In Review`.