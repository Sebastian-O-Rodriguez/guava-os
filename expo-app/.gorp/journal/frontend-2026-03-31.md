## Task Wave5 — Wire chat screen, Motion animations, not-found route

Status: done
Files:
- app/(tabs)/chat.tsx (created)
- app/(tabs)/_layout.tsx (modified — added Chat tab)
- app/+not-found.tsx (created)
- app/(tabs)/index.tsx (modified — motion fade-in entrance on metrics area)
- components/dashboard/liquid-gauge.tsx (modified — whileTap scale on web gauge)
- components/dashboard/metrics-card.tsx (modified — motion stagger entrance on web gauges)

Tests: n/a (vitest not installed per known issue I1)

Summary:

Task 1 — Chat screen wired as a third tab. Created app/(tabs)/chat.tsx rendering the existing Chat component full-screen, with a dark background and max-width container on web and a native YStack layout on iOS/Android. Added `<Tabs.Screen name="chat" options={{ title: "Chat" }} />` to the tabs layout so it appears in the bottom tab bar.

Task 2 — Motion microinteractions (web only):
- index.tsx: MetricsCard area and empty-state div are wrapped in `motion.div` with `initial={{ opacity: 0, y: 16 }}` / `animate={{ opacity: 1, y: 0 }}` fade-slide entrance (0.35s easeOut).
- metrics-card.tsx: Web branch now builds a `gaugeItems` array and renders each in a `motion.div` with `variants` for stagger (0.07s between children, 0.3s per item, easeOut). Outer container is `motion.div` with `staggerChildren`. Native branch unchanged, still uses the existing `gauges` fragment.
- liquid-gauge.tsx: The clickable gauge circle in `LiquidGaugeWeb` is now a `motion.div` (was plain div) with `whileTap={{ scale: 0.95 }}` for tap-press feedback. Removed the CSS `transition: "transform 150ms"` since Motion owns the transform now. Closing tag updated to `</motion.div>`.

Task 3 — Created app/+not-found.tsx with Tamagui YStack centered layout, "Page not found" heading, and a "Go to Dashboard" button using `router.replace("/")`.

Screenshots:
- Dashboard web: metrics card slides up and fades in on data load; each gauge pops in sequentially with stagger
- Gauge press: circles scale down to 0.95 on click, spring back on release
- Chat tab: third tab in bottom bar labeled "Chat"; full-screen chat UI with header and Chat component
- Not found: centered dark screen with text and navigation button

Blockers: none
