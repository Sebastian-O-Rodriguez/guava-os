# ⛔ legacy/linear/ — LEGACY — DO NOT USE

Linear-coupled spec copies, removed from this repo's active `.gorp/` material.

- **LEGACY — DO NOT USE.**
- **Not canonical.** Canonical contracts are source-neutral and live in Gorp
  (`~/dev/gorp/specs/runtime/*` + `~/dev/gorp/specs/resolved-context-contract.md`).
- **Not loaded.** Nothing in this repo or the Gorp loader reads these.
- **Retained for future adapter reference only.** If a Linear import adapter is
  built later, these are starting-point source material. Linear is not core.

Contents (moved from `.gorp/specs/`): `graph-semantics.md`,
`execution-state-machine.md`, `claim-leases.md`, `violation-codes.md`.
