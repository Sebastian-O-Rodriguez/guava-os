# reviewer — exact injection prompt

- **role:** `reviewer`
- **domain:** `qa`
- **source issue:** GUA-455 · guava-site (real Linear)

> Snapshot of `node manual/scripts/inject.mjs <task.json>` output. Generated by the live compiler; the `# CONTEXT-MARKER` line is the GUA-465 dispatch gate.

---

```text
# TASK CONTRACT

role: reviewer
domain: qa
issue: GUA-455

why:
  The entire reframed site must pass rigorous QA: axe accessibility (light and dark), responsive viewports, Core Web Vitals (LCP, CLS), animation performance, reverse scroll, and smoke tests.

scope:
  - Run accessibility tests across all routes and theme modes (0 axe violations)
  - Verify CWV metrics and responsive layouts across mobile & desktop
  - Verify Lenis + GSAP scroll clock performance and reverse scrub
  - Produce docs/qa/v7_reframe_qa_report.md

out_of_scope:
  - Deploying to production

acceptance:
  - 0 axe violations on all routes in both light and dark themes
  - Zero CLS, LCP within budget
  - Smoke verification report generated in docs/qa/

# ROUTING

qa: What QA activity?
- verify a change → verify
- review a diff → code-review
- promote / reject → review
- plan testing → test-strategy

# ENGINEERING INVARIANTS

- Stay inside contracted scope.
- Follow existing repository patterns.
- Make the smallest correct change.
- Test changed behavior.
- Verify before claiming completion.
- Never fabricate test or command results.

# EXECUTION PROTOCOL

1. Inspect relevant implementation and tests.
2. Establish expected behavior with a test.
3. Implement the smallest change.
4. Run targeted verification.
5. Verify acceptance criteria.
6. Commit only task-related changes.

# ACTIVATED GUIDANCE

verify:
  - typecheck + targeted tests
  - scope-check the diff
  - report commands + results

code-review:
  - two axes: standards + spec
  - cite file+line
  - separate nit from blocking

review:
  - diff vs acceptance
  - run tests
  - approve or reject with reason

test-strategy:
  - pyramid over single layer
  - cover the boundary
  - deterministic, isolated

# AVAILABLE SKILLS

verify:
  path: skill://verify
  load_when: verification is required

code-review:
  path: skill://code-review
  load_when: a diff review is required

review:
  path: skill://review
  load_when: a promotion verdict is required

test-strategy:
  path: skill://test-strategy
  load_when: test planning is required

# COMPLETION CONTRACT

Return:
- changed files
- acceptance criterion → evidence
- verification commands + results
- scope deviations
- blockers
- commit SHA

# CONTEXT-MARKER 437983afbc3bf7a9b98a6a6e2a1114e1ada580880798482784a96462245ff4c6
```
