# task — exact injection prompt

- **role:** `task`
- **domain:** `backend`
- **source issue:** GUA-91 · reusable-diagnostic-engine (real Linear)

> Snapshot of `node manual/scripts/inject.mjs <task.json>` output. Generated by the live compiler; the `# CONTEXT-MARKER` line is the GUA-465 dispatch gate.

---

```text
# TASK CONTRACT

role: task
domain: backend
issue: GUA-91

why:
  Render client-facing diagnostic outputs (Executive Summary, Current State, Key Findings, Projects/Priorities, Recommended Next Step, and Bell-demonstrated variants) from structured state — document-first outputs are NOT the source of truth.

scope:
  - Render core outputs from structured diagnostic state (state is canonical)
  - One rendering path producing multiple documents from the same state
  - Client-readable: jargon-light, few important problems, problem→evidence→implication, clear next step (commercial quality bar)
  - HTML + optional PDF via the same path (WeasyPrint mechanism, not Bell content)

out_of_scope:
  - No separate disconnected reasoning per document
  - No web app

acceptance:
  - Output-rendering module (state→docs) + templates + tests

# ROUTING

backend: What kind of backend work?
- new / changed API → api-design → python-backend
- schema / query / migration → sql-postgres → supabase → supabase-postgres-best-practices

# ENGINEERING INVARIANTS

- Stay inside contracted scope.
- Follow existing repository patterns.
- Make the smallest correct change.
- Test changed behavior.
- Verify before claiming completion.
- Never fabricate test or command results.

# EXECUTION PROTOCOL

1. Inspect relevant implementation and tests.
2. Establish expected behavior with a test.
3. Implement the smallest change.
4. Run targeted verification.
5. Verify acceptance criteria.
6. Commit only task-related changes.

# ACTIVATED GUIDANCE

api-design:
  - preserve existing API conventions
  - use existing error format
  - avoid unrelated API changes

python-backend:
  - match existing FastAPI/Django patterns
  - reuse existing models and services
  - add type hints and a test

sql-postgres:
  - follow the existing DB abstraction
  - inspect the query plan before perf claims
  - no schema/index changes without evidence

supabase:
  - use the existing Supabase client
  - respect RLS
  - add tables only via migrations

supabase-postgres-best-practices:
  - RLS on all tables
  - migrations not ad-hoc SQL
  - index where queries filter

# AVAILABLE SKILLS

api-design:
  path: skill://api-design
  load_when: deeper API semantics are required

python-backend:
  path: skill://python-backend
  load_when: framework-specific implementation guidance is needed

sql-postgres:
  path: skill://sql-postgres
  load_when: query/index/schema decisions require deeper guidance

supabase:
  path: skill://supabase
  load_when: the repo implementation actually uses Supabase

supabase-postgres-best-practices:
  path: skill://supabase-postgres-best-practices
  load_when: schema/RLS/migration best practices are needed

# COMPLETION CONTRACT

Return:
- changed files
- acceptance criterion → evidence
- verification commands + results
- scope deviations
- blockers
- commit SHA

# CONTEXT-MARKER 1c0954155cf441830e92c73b75e3e4f505fb0cd4e0e907aed7e649d8ad0f1a64
```
