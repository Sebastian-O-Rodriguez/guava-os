# librarian — exact injection prompt

- **role:** `librarian`
- **domain:** `frontend`
- **source issue:** GUA-464 · guava-site (real Linear)

> Snapshot of `node manual/scripts/inject.mjs <task.json>` output. Generated by the live compiler; the `# CONTEXT-MARKER` line is the GUA-465 dispatch gate.

---

```text
# TASK CONTRACT

role: librarian
domain: frontend
issue: GUA-464

why:
  Before authoring the production homepage visual-architecture .riv, prove the agent can directly access Rive tooling end-to-end. This de-risks GUA-451.

scope:
  - Prove direct access to Rive tooling (Rive MCP / Rive Editor)
  - Create or edit a TRIVIAL Rive artifact (not the production guava composition)
  - Export a real, valid .riv binary and load it in the repo/runtime (Rive WASM)
  - Record concrete evidence (artifact path, load output) in issue comments

out_of_scope:
  - No Canvas substitute
  - No production animation content

acceptance:
  - A real .riv file exists and loads in the repo/runtime
  - Toolchain access + successful load evidence recorded
  - Unblocks GUA-451

# ROUTING

frontend: What kind of frontend work?
- app UI / components → react-nextjs
- animation → gsap-core → gsap-timeline → gsap-scrolltrigger → rive
- presentation → reveal-presentation
- component API design → vercel-composition-patterns → vercel-react-best-practices

# ENGINEERING INVARIANTS

- Stay inside contracted scope.
- Follow existing repository patterns.
- Make the smallest correct change.
- Test changed behavior.
- Verify before claiming completion.
- Never fabricate test or command results.

# EXECUTION PROTOCOL

1. Inspect relevant implementation and tests.
2. Establish expected behavior with a test.
3. Implement the smallest change.
4. Run targeted verification.
5. Verify acceptance criteria.
6. Commit only task-related changes.

# ACTIVATED GUIDANCE

react-nextjs:
  - prefer server components unless client state is required
  - reuse existing component patterns
  - use the existing data-fetching approach

gsap-core:
  - use the installed gsap
  - prefer transform over layout properties
  - clean up on unmount

gsap-timeline:
  - one timeline per sequence
  - use the position parameter
  - clean up on unmount

gsap-scrolltrigger:
  - use ScrollTrigger over scroll listeners
  - pin only what must pin
  - respect prefers-reduced-motion

gsap-plugins:
  - register the plugin once
  - prefer built-in over custom
  - verify the plugin is installed

gsap-react:
  - use useGSAP + context
  - scope selectors to refs
  - clean up in the same effect

gsap-utils:
  - prefer gsap.utils over hand-rolled
  - clamp/mapRange over ad-hoc math
  - one-liner over loop

gsap-frameworks:
  - scope to the component lifecycle
  - clean up on unmount
  - match the framework's mount pattern

gsap-performance:
  - animate transform/opacity only
  - batch reads before writes
  - avoid layout thrash

rive:
  - load via the rive runtime
  - drive via state machines
  - match the existing integration

reveal-presentation:
  - nested slides + markdown
  - one idea per slide
  - export to PDF if asked

vercel-composition-patterns:
  - compound components over boolean props
  - single responsibility
  - compose, don't prop-drill

vercel-react-best-practices:
  - memoize at the right boundary
  - avoid re-render waterfalls
  - measure before optimizing

vercel-react-native-skills:
  - follow existing native patterns
  - FlatList for long lists
  - test on the target platform

vercel-react-view-transitions:
  - use the ViewTransition component
  - match existing transition types
  - respect reduced motion

# AVAILABLE SKILLS

react-nextjs:
  path: skill://react-nextjs
  load_when: React/Next.js implementation guidance is needed

gsap-core:
  path: skill://gsap-core
  load_when: animation work is in scope

gsap-timeline:
  path: skill://gsap-timeline
  load_when: sequenced animation is in scope

gsap-scrolltrigger:
  path: skill://gsap-scrolltrigger
  load_when: scroll-linked animation is in scope

gsap-plugins:
  path: skill://gsap-plugins
  load_when: a gsap plugin feature is in scope

gsap-react:
  path: skill://gsap-react
  load_when: gsap in a React component is in scope

gsap-utils:
  path: skill://gsap-utils
  load_when: a gsap helper is needed

gsap-frameworks:
  path: skill://gsap-frameworks
  load_when: gsap in a non-React framework is in scope

gsap-performance:
  path: skill://gsap-performance
  load_when: animation performance matters

rive:
  path: skill://rive
  load_when: Rive animation is in scope

reveal-presentation:
  path: skill://reveal-presentation
  load_when: a slide deck is a deliverable

vercel-composition-patterns:
  path: skill://vercel-composition-patterns
  load_when: component API design is in scope

vercel-react-best-practices:
  path: skill://vercel-react-best-practices
  load_when: React performance guidance is needed

vercel-react-native-skills:
  path: skill://vercel-react-native-skills
  load_when: mobile React work is in scope

vercel-react-view-transitions:
  path: skill://vercel-react-view-transitions
  load_when: page/route transitions are in scope

# COMPLETION CONTRACT

Return:
- changed files
- acceptance criterion → evidence
- verification commands + results
- scope deviations
- blockers
- commit SHA

# CONTEXT-MARKER 7473b3bcd0963a66161c43dc246d5bc3957d467dd0c35734065a6a6935592533
```
