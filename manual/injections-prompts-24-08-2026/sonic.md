# sonic — exact injection prompt

- **role:** `sonic`
- **domain:** `devops`
- **source issue:** GUA-414 · guava-site (real Linear)

> Snapshot of `node manual/scripts/inject.mjs <task.json>` output. Generated by the live compiler; the `# CONTEXT-MARKER` line is the GUA-465 dispatch gate.

---

```text
# TASK CONTRACT

role: sonic
domain: devops
issue: GUA-414

why:
  Make guavaai.ai the sole canonical domain and 301-redirect the old guavatt.com and www.guavatt.com to it.

scope:
  - astro.config.mjs site = https://guavaai.ai
  - all src/pages/*.astro url constants and og-image fallbacks emit guavaai.ai
  - public/_redirects 301-redirects guavatt.com and www.guavatt.com (apex and subpaths) to guavaai.ai

out_of_scope:
  - Cal.com guavatt/30min scheduling slug and the contact mailbox — separate systems outside this issue

acceptance:
  - canonical + og:url + sitemap entry URLs resolve to guavaai.ai after astro build
  - no guavatt.com references left in source except the Cal.com slug and contact mailbox

# ROUTING

devops: What kind of DevOps work?
- CI/CD → ci-cd
- infra as code → terraform
- observability → observability
- deploy / platform → deploy-to-vercel → vercel → vercel-cli-with-tokens

# ENGINEERING INVARIANTS

- Stay inside contracted scope.
- Follow existing repository patterns.
- Make the smallest correct change.
- Test changed behavior.
- Verify before claiming completion.
- Never fabricate test or command results.

# EXECUTION PROTOCOL

1. Inspect relevant implementation and tests.
2. Establish expected behavior with a test.
3. Implement the smallest change.
4. Run targeted verification.
5. Verify acceptance criteria.
6. Commit only task-related changes.

# ACTIVATED GUIDANCE

ci-cd:
  - reuse existing workflow patterns
  - keep gates deterministic
  - test the workflow locally

terraform:
  - reuse existing modules
  - plan before apply
  - keep state remote

observability:
  - reuse existing instrumentation
  - one metric per decision
  - alert on symptoms not causes

vercel:
  - reuse existing project config
  - check env vars
  - preview before prod

vercel-cli-with-tokens:
  - use the token, never interactive login
  - scope commands
  - verify against the right project

deploy-to-vercel:
  - build before deploy
  - verify preview
  - return the URL

vercel-optimize:
  - collect metrics first
  - investigate only metric-backed candidates
  - measure before/after

# AVAILABLE SKILLS

ci-cd:
  path: skill://ci-cd
  load_when: CI/CD pipeline work is in scope

terraform:
  path: skill://terraform
  load_when: infrastructure-as-code is in scope

observability:
  path: skill://observability
  load_when: metrics/logs/tracing is in scope

vercel:
  path: skill://vercel
  load_when: Vercel platform work is in scope

vercel-cli-with-tokens:
  path: skill://vercel-cli-with-tokens
  load_when: Vercel CLI with a token is needed

deploy-to-vercel:
  path: skill://deploy-to-vercel
  load_when: a Vercel deploy is the deliverable

vercel-optimize:
  path: skill://vercel-optimize
  load_when: Vercel cost/perf optimization is in scope

# COMPLETION CONTRACT

Return:
- changed files
- acceptance criterion → evidence
- verification commands + results
- scope deviations
- blockers
- commit SHA

# CONTEXT-MARKER 4d377352c1fe2a4bddaf1a445f046a7cc9ece4ccd965d0b2eda36b6c0affa862
```
