---
title: Skills
description: "The map: every skill, its domain, dispatching agent, and when to load it."
---

# Skills

Canonical store: `~/.agents/skills/<name>/SKILL.md` (generated by `manual/scripts/gen.mjs`). Each domain links to its full-content page.

## [Core](/core/) (7)

| Skill | Agent | load_when |
|---|---|---|
| `engineering-principles` | any | Core engineering red lines for every worker: prove it works, fix root causes, build the lever, sequence verifiable units, protect boundaries, keep the context window small. Apply to any implementation, refactor, migration, or review. |
| `tdd` | any | Test-driven development: write a failing test first, make it green, refactor. Use for any feature or bug fix that has a cheap local test path. |
| `diagnosing-bugs` | any | Disciplined root-cause loop for hard bugs and regressions: reproduce, isolate, hypothesize, instrument, fix, regression-test. Use when the cause isn't obvious. |
| `writing-for-agents` | any | Author documents agents reach by pointer — skills, AGENTS.md/CLAUDE.md, runbooks, context docs — so they're loadable, scannable, and machine-actionable. Use when writing or reviewing any agent-facing doc. |
| `technical-writing` | any | Layered doc standard (Diátaxis structure + concise style) for docs, RFCs, READMEs, PR descriptions, and commit messages. Use for any prose deliverable. |
| `diagrammatic-writing` | any | Compose documents where spatial layout (hierarchy, margins, position, proximity) carries the argument, not just linear prose. Use for architecture docs, plans, reports, and any dense agent-authored document. |
| `diagrammatic-review` | any | Audit a document's spatial structure against diagrammatic-writing criteria: does the layout (hierarchy, position, proximity) carry or contradict the argument? Use to review docs, plans, reports, and indexes. |

## [PM](/roles/pm/) (8)

| Skill | Agent | load_when |
|---|---|---|
| `grilling` | manager | Relentlessly interview about a plan or design until every branch is resolved, building a shared domain language (CONTEXT.md / glossary) as you go. Use before implementing anything non-trivial. |
| `to-tickets` | manager | Break a plan, spec, or conversation into tracer-bullet tickets with explicit blocking edges, written as Linear issues or a local file. Use after planning, before dispatch. |
| `planning` | manager | Sprint planning and board health — the guava-os default planning pattern. guava-os decomposes into scoped Linear deliverables; OMP subagents execute; writes go through the linear skill. |
| `linear` | manager | Project management via guava-os tooling — the standard agent interface for Linear. Prefer pm tooling; reach for Linear MCP only as a fallback. Linear is the workflow state of record. |
| `dispatch` | manager | Project-session dispatcher — load this repo's open Linear issues and delegate each to an OMP role subagent. guava-os planned; the subagents execute. |
| `handoff` | manager | Read/write session handoff notes — the Linear issue + comment thread is the state of record. |
| `add-skill` | manager | Add a new skill to this workspace's canonical skill store and wire it into consumers (omp, claude). Use when asked to add/install/download a skill, make a skill available, or after fetching a skill via the skills ecosystem (npx skills add). Provenance: single source of truth for skills is ~/.agents/skills; every consumer (project .omp/skills, project .claude/skills, ~/.claude/skills) references it via symlinks; no duplicate real copies live anywhere else. |
| `context-assembly` | manager | Assemble a worker's context as a compiler: small stable core + explicit task contract + activated guidance + progressive retrieval + measurable verification. Use to build the prompt a dispatched worker receives. |

## [QA](/roles/qa/) (4)

| Skill | Agent | load_when |
|---|---|---|
| `verify` | reviewer | verification is required |
| `code-review` | reviewer | a diff review is required |
| `review` | reviewer | a promotion verdict is required |
| `test-strategy` | reviewer | test planning is required |

## [Security](/roles/security/) (2)

| Skill | Agent | load_when |
|---|---|---|
| `secure-coding` | security-reviewer | security-sensitive code is in scope |
| `security-review` | security-reviewer | a security audit is required |

## [Backend](/roles/backend/) (5)

| Skill | Agent | load_when |
|---|---|---|
| `api-design` | task | deeper API semantics are required |
| `python-backend` | task | framework-specific implementation guidance is needed |
| `sql-postgres` | task | query/index/schema decisions require deeper guidance |
| `supabase` | task | the repo implementation actually uses Supabase |
| `supabase-postgres-best-practices` | task | schema/RLS/migration best practices are needed |

## [Frontend / Designer](/roles/frontend/) (15)

| Skill | Agent | load_when |
|---|---|---|
| `react-nextjs` | designer | React/Next.js implementation guidance is needed |
| `gsap-core` | designer | animation work is in scope |
| `gsap-timeline` | designer | sequenced animation is in scope |
| `gsap-scrolltrigger` | designer | scroll-linked animation is in scope |
| `gsap-plugins` | designer | a gsap plugin feature is in scope |
| `gsap-react` | designer | gsap in a React component is in scope |
| `gsap-utils` | designer | a gsap helper is needed |
| `gsap-frameworks` | designer | gsap in a non-React framework is in scope |
| `gsap-performance` | designer | animation performance matters |
| `rive` | designer | Rive animation is in scope |
| `reveal-presentation` | designer | a slide deck is a deliverable |
| `vercel-composition-patterns` | designer | component API design is in scope |
| `vercel-react-best-practices` | designer | React performance guidance is needed |
| `vercel-react-native-skills` | designer | mobile React work is in scope |
| `vercel-react-view-transitions` | designer | page/route transitions are in scope |

## [DevOps](/roles/devops/) (7)

| Skill | Agent | load_when |
|---|---|---|
| `ci-cd` | task | CI/CD pipeline work is in scope |
| `terraform` | task | infrastructure-as-code is in scope |
| `observability` | task | metrics/logs/tracing is in scope |
| `vercel` | task | Vercel platform work is in scope |
| `vercel-cli-with-tokens` | task | Vercel CLI with a token is needed |
| `deploy-to-vercel` | task | a Vercel deploy is the deliverable |
| `vercel-optimize` | task | Vercel cost/perf optimization is in scope |

## [AI / ML](/roles/ai-ml/) (3)

| Skill | Agent | load_when |
|---|---|---|
| `rag` | task | retrieval/vector work is in scope |
| `prompt-engineering` | task | prompt design/eval is in scope |
| `pandas-data` | task | dataframe work is in scope |

## Ecosystem / unassigned (7)

| Skill | Agent | load_when |
|---|---|---|
| `cavecrew` | — | > |
| `caveman` | — | > |
| `caveman-commit` | — | > |
| `caveman-compress` | — | > |
| `caveman-help` | — | > |
| `caveman-review` | — | > |
| `caveman-stats` | — | > |

